#-------------------------------------------------------------------------------
# Ruby
#-------------------------------------------------------------------------------

path put ~/.gem/ruby/2.2.0/bin

[[ -d "${USR_PATH}/share/chruby" ]] && {
  for f in "${USR_PATH}"/share/chruby/*; do
    . "${f}"
  done
}
